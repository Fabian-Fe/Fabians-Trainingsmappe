#include <iostream>
#include <string.h>
using namespace std;

void swap(int,int);


int main() 
{
	int lotto[] = { 42,3,49,12 };
	int posListe[4];
	//outer loop

		// for loop with i
	if (lotto[0] < lotto[1])	
		int smallest = i
	// end loop, smalles of round determined
		posListe [j]= smallest
		//outer loop

		//rearrangeloop
		for (i = 0; i < length; i++)
		{
			swap (lotto[i],posListe)
		}
		swap
		
}

void swap(int a,int b)
{
	
		
}
